<% include ./partials/header.ejs %>
    <div class="w-full min-h-screen bg-zinc-800 text-white">
        <div class="cards flex flex-wrap gap-10">
            <% user.posts.forEach((element) => { 
                console.log(element);
            %>
            <div class="card w-32 overflow-hidden">
                <div class="h-40 w-full bg-sky-200 rounded-lg">
                    <img class="w-full h-full object-cover" src="/images/uploads/<%= element.image %>" alt="">
                </div>
                <h5 class="mt-2 text-sm"><%= element.title%><h5>
                <p class="mt-2 text-sm"> <%= element.description%> </p>
            </div>
            <%   }); %>
        </div>
    </div>
<% include ./partials/footer.ejs %>
